<h1>List of Messages</h1>

<p><%= link_to "Send new text", new_message_path %></p>

<ul>
  <% @messages.each do |message| %>
    <li>To: <%= message.to %>, From: <%= message.from %>, Body: <%= message.body %></li>
  <% end %>
</ul>
